from email import header
import requests



from functools import partial
print = partial(print, flush=True)

async def expired():
    url = ''
    header = {
        
    }
    result =  requests.get()